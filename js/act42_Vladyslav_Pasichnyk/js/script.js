function canvia(elem) {
   elem.style.backgroundImage = 'url(./img/llumon.gif)';
   }

function desfe(elem) {
    elem.style.backgroundImage = 'url(./img/llumoff.gif)';
}

function destroy(elem) {
    elem.style.backgroundImage = 'url(./img/llumbreak.gif)';
}